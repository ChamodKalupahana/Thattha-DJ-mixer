[] add github actions to deploy on main commit
[] investigate if github actions can run tests and checks on pull requests
[] player in mixer mode should be smaller so that playback seek buttons and video can fit onto screen size
[] need to be able to upload new clips while already in mixer mode, should give warning if already in library